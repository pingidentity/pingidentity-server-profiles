${BASE:-/opt}
administrator
/opt/java
${BASE}/staging
${BASE}/out
${OUT_DIR}/instance
${BASE}/in
${BASE}/server
${BASE}/backup
${BASE}/logs
NO
devops-secret
${BASE}/staging-manifest.txt
false
/run/secrets
${STAGING_DIR}/topology.json
${STAGING_DIR}/hooks
${STAGING_DIR}/.env
/tmp/server-profile
true
.jwk .pin
${SERVER_ROOT_DIR}
${STAGING_DIR}/pd.profile/server-root/pre-setup
Docker
384m
AGGRESSIVE
75.0
1389
1636
1443
1689
dc=example,dc=com
'$'
localhost
pingdirectory
pingdirectoryproxy
pingdatasync
pingauthorize
pingauthorizepap
pingfederate
https://localhost:9999
pingfederate-admin
pingaccess
pingaccess-admin
cn=${ROOT_USER}
${BASE}/.profile
\${PING_PRODUCT}:\h:\w\n>
${JAVA_HOME}/bin:${BASE}:${SERVER_ROOT_DIR}/bin:${PATH}
${SHIM}
${IMAGE_VERSION}
${IMAGE_GIT_REV}
${DATE}
${VERSION}
PingFederate
${SERVER_ROOT_DIR}/server/default/conf
pingfederate.lic
PF
${LICENSE_VERSION}
${SERVER_ROOT_DIR}/bin/run.sh
2FederateM0re
${SERVER_ROOT_DIR}/log/server.log
10000 KB
2
9999
9031
-1
Defines a secondary HTTPS port that can be used for mutual SSL/TLS (client X.509 certificate) authentication for both end users and protocol requests. PF_RUN_PF_SECONDARY_HTTPS_PORT (default 9032) will override this value. The default value of -1 disables the port in the product.
PF_ENGINE_DEBUG
9030
STANDALONE
OFF
PingDirectory
NON_LOOPBACK
1
600
512
Docker PingFederate
data.json
ADMIN_WAITFOR_TIMEOUT
300

